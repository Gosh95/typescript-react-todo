import React, { Fragment } from 'react';
import TodosContainer from './components/Todos/TodosContainer';

const App: React.FC = () => {
  return (
    <Fragment>
      <TodosContainer />
    </Fragment>
  );
};

export default App;
